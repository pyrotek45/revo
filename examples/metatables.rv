const Point_mt = {
    __tostring = fn(self) fmt("(%v, %v)", self.x, self.y),
    __add      = fn(a, b) set_metatable({x = a.x + b.x, y = a.y + b.y}, Point_mt),
    dist       = fn(self) math.sqrt(self.x * self.x + self.y * self.y),
}

const p1 = set_metatable({x = 3, y = 0}, Point_mt)
const p2 = set_metatable({x = 0, y = 4}, Point_mt)
print(tostring(p1 + p2)) # (3, 4)
print((p1 + p2):dist())  # 5
