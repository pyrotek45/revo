const a = comp macro do
	"1 + 1"
end

print(a)

const x = do
	let res = io:read():unwrap()
		|> fn(c) c:upper()

	match res
	| "HELLO" when io:read():unwrap():len() < 2
		{}:set_meta({
				__index = fn(self, k) k * 2
		})
	| x
	when do
		let u = x
		print(u)
	end :ok
end

print(x)
